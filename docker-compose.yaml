version: '3.1'

services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: admin
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - 3306:3306

  go:
    build: ./src
    volumes:
      - ./src:$GOPATH/src/go_books/src
    ports:
      - 8080:8080
    links:
      - db



volumes:
  db_data:
